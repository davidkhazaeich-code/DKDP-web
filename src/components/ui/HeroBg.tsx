/**
 * HeroBg — re-exports HeroBgInteractive so all existing imports automatically
 * get the cursor-tracking illumination effect with no code changes needed.
 */
export { HeroBgInteractive as HeroBg } from './HeroBgInteractive'
